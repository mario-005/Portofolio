# Portfolio — Benediktus Mario Laksono

This is a small interactive portfolio site (static) built with HTML, CSS, and JavaScript. Theme: red & black, with a focus on JS interactions.

Files added:
- `index.html` — main page
- `css/styles.css` — styles
- `js/script.js` — interactive behaviors

How to open:
1. Open the folder `portfolio-site` in your file explorer.
2. Double-click `index.html` or open it with your browser (Chrome, Edge, Firefox).

Notes & next steps:
- Replace placeholder project details with real projects and links (GitHub, live demos).
- If you have a `resume.pdf`, place it in the same folder and wire the download button in `js/script.js`.
- You can customize colors in `css/styles.css` (root variables).

If you'd like, I can:
- Add image assets and favicons
- Convert contact form to send real messages via a serverless function
- Add a build pipeline or React/Next.js version
